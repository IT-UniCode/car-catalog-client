import React from 'react';

import useStyles from './style';

const CalcCustoms = () => {
  const classes = useStyles();

  return (
    <div className={classes.root}>
      CALC
    </div>
  );
};

export default CalcCustoms;
